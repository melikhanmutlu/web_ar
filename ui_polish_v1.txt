UI Polish - Phase 1

Changes Made:

1. Remove Emojis from Panel Headers
   - Material Editor: 🎨 → (removed)
   - Transform: 🔧 → (removed)
   - Annotations: 📍 → (removed)
   - Cleaner, more professional look

2. Elegant Button Redesign
   - Replaced bright colored buttons with elegant styles
   - Added three button classes:
     * btn-primary: Purple gradient, white text
     * btn-secondary: Light border, subtle hover
     * btn-danger: Red border, light background
   - Theme-aware (light/dark mode support)
   - Smooth hover animations (translateY, shadow)
   - Applied to all panel buttons:
     * Add Hotspot
     * Measure Distance
     * Clear All
     * Reset Material
     * Reset Transform
     * Upload Texture
     * Remove Texture
     * Export Modified Model
     * Save & Apply to AR

3. Model Size Limit Check
   - Confirmed apply_size_limit() function exists in app.py
   - Currently disabled (commented out) for debugging
   - Function scales model to max_size_meters (default 0.35m)
   - Maintains proportions
   - Works with both Scene and Trimesh objects
   - Ready to be re-enabled when needed

Next Steps:
- Add scale tracking with dimension display
- Update dimensions when scale changes
- Improve annotations with interactive pins
- Add custom camera views/bookmarks
