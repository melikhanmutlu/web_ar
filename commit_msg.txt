Fix Railway deployment: Add Dockerfile, nixpacks.toml, and production-safe migration

- Add Dockerfile with Python 3.11 and Node.js
- Add nixpacks.toml for Railway build configuration
- Add .dockerignore for faster builds
- Update migrate_db.py to be production-safe (no drop tables)
- Migration now exits gracefully on error
